---
# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: 3

tasks:
  zizmor:
    desc: Analyse GitHub Workflows and Actions with zizmor
    summary: |-
      Run zizmor against the GitHub Workflows and GitHub Actions configurations to identify any potential issues with
      artifact integrity, excessive permissions, injection weaknesses, secrets exposure weaknesses, trigger misuse, and
      unpinned dependencies.
    silent: true
    cmds:
      - defer: "echo -e {{ if .EXIT_CODE }}'\e[1;31mFailed\e[0m'{{ else }}'\e[0;32mCompleted\e[0m'{{ end }}"
      - task: zizmor:workflows
      - task: zizmor:dependabot

  zizmor:workflows:
    desc: Analyse the GitHub Workflows with zizmor
    summary: |-
      Run zizmor against the GitHub Workflows to identify any potential issues with artifact integrity, excessive
      permissions, injection weaknesses, secrets exposure weaknesses, trigger misuse, and unpinned dependencies.
    sources:
      - .github/workflows/*.yaml
      - .github/zizmor.yaml
    silent: true
    deps:
      - task: check:exec:zizmor
    cmds:
      - defer: "echo -e {{ if .EXIT_CODE }}'\e[1;31mFailed\e[0m'{{ else }}'\e[0;32mPassed\e[0m'{{ end }}"
      - if: find .github/workflows -type f -name '*.yaml' >dev/null 2>&1
        cmd: zizmor --persona auditor --no-progress --format plain .github/workflows/*.yaml

  zizmor:dependabot:
    desc: Analyse the GitHub Dependabot configuration with zizmor
    summary: |-
      Run the static analysis tool `zizmor` against the GitHub Dependabot configuration to identify any potential
      misconfiguration issues or security weaknesses within the workflows.
    sources:
      - .github/dependabot.yaml
      - .github/zizmor.yaml
    silent: true
    deps:
      - task: check:exec:zizmor
    cmds:
      - defer: "echo -e {{ if .EXIT_CODE }}'\e[1;31mFailed\e[0m'{{ else }}'\e[0;32mPassed\e[0m'{{ end }}"
      - if: test -f .github/dependabot.yaml
        cmd: zizmor --persona auditor --no-progress --format plain .github/dependabot.yaml
